import {View, Text} from 'react-native';
import React from 'react';

const ShareView = () => {
  return (
    <View style={{flex: 1}}>
      <Text>Share View</Text>
    </View>
  );
};

export default ShareView;
